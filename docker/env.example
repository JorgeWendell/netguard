# Este arquivo foi substituído por .env.docker.example na raiz do projeto.
# Use o mesmo padrão dos outros projetos (adelcrm, fsnext3, etc.):
#
#   cp .env.docker.example .env
#
# O docker-compose monta DATABASE_URL automaticamente com host "db".
